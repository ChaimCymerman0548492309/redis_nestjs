type Garden {
  # Example field (placeholder)
  exampleField: Int
}

input CreateGardenInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateGardenInput {
  id: Int!
}

type Query {
  gardens: [Garden]!
  garden(id: Int!): Garden
}

type Mutation {
  createGarden(createGardenInput: CreateGardenInput!): Garden!
  updateGarden(updateGardenInput: UpdateGardenInput!): Garden!
  removeGarden(id: Int!): Garden
}
